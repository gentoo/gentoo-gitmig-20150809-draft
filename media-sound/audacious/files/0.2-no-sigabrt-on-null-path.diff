--- audacious-0.2/audacious/skin.c.orig	2006-02-18 19:23:03.000000000 +0000
+++ audacious-0.2/audacious/skin.c	2006-02-18 19:29:46.000000000 +0000
@@ -837,7 +837,9 @@ skin_load(Skin * skin, const gchar * pat
     gboolean error;
 
     g_return_val_if_fail(skin != NULL, FALSE);
-    g_return_val_if_fail(path != NULL, FALSE);
+
+    if (!path)
+        return FALSE;
 
     skin_lock(skin);
     error = skin_load_nolock(skin, path, FALSE);
