--- awstats-6.1.orig/tools/configure.pl.orig	2004-05-18 14:17:42.000000000 +0200
+++ awstats-6.1/tools/configure.pl	2004-05-18 14:19:29.000000000 +0200
@@ -645,8 +645,8 @@
 # ----------------------------------
 if ($WebServerChanged) {
 	if ($OS eq 'linux') 	{
-		print "\n-----> Restart Web server with '/sbin/service httpd restart'\n";
-	 	my $ret=`/sbin/service httpd restart`;
+		print "\n-----> Restart Web server graceful with '/usr/sbin/apache2ctl graceful'\n";
+	 	my $ret=`/usr/sbin/apache2ctl graceful`;
 	 	print "$ret";
 	}
 	elsif ($OS eq 'macosx')	{
