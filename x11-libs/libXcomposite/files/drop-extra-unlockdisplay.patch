--- src/Xcomposite.c
+++ src/Xcomposite.c
@@ -235,8 +235,6 @@ XCompositeQueryVersion (Display *dpy,
     XCompositeCheckExtension (dpy, info, 0);
     *major_versionp = info->major_version;
     *minor_versionp = info->minor_version;
-    UnlockDisplay (dpy);
-    SyncHandle ();
     return 1;
 }
