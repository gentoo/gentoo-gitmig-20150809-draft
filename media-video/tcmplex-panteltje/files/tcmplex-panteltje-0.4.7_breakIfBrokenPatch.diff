based on
http://ben.amasoean.de/articles/2006/02/26/69_korrupte-Daten.html

Joerg Bornkessel <hd_brummy@gentoo.org> 28 Jun 2006

diff -Naur tcmplex-panteltje-0.4.7.orig/inptstrm.c tcmplex-panteltje-0.4.7/inptstrm.c
--- tcmplex-panteltje-0.4.7.orig/inptstrm.c	2006-06-28 15:12:07.963045000 +0200
+++ tcmplex-panteltje-0.4.7/inptstrm.c	2006-06-28 15:14:03.986296000 +0200
@@ -843,6 +843,13 @@
       break;
   } while (!end_bs(&bs));
 
+  // will break if converted file broken on filesize
+  if (percent<99) {
+            sprintf(tmpStr, "Error reading file: This File is corrupted.");
+            DisplayError(tmpStr);
+       goto exit2;
+    }
+
   if (!AbortMPEG)
   {
     sprintf(tmpStr, "  Found %d picture headers, stream bit offset %.0f.",
