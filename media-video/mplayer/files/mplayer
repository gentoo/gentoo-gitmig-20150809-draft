#!/bin/sh

mplayerbin=`which mplayer-bin`

if [ ! -d ~/.mplayer ] ; then
	mkdir ~/.mplayer
fi

if [ ! -f ~/.mplayer/codecs.conf ] ; then
	cp /etc/codecs.conf ~/.mplayer/codecs.conf
fi

$mplayerbin $@

