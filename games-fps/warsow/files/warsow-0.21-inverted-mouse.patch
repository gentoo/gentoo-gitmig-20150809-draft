--- source/linux/in_x11.c
+++ source/linux/in_x11.c
@@ -589,10 +589,8 @@
 					}
 					else
 					{
-						mx = -((int)event.xmotion.x - mwx);
-						my = -((int)event.xmotion.y - mwy);
-						mwx = event.xmotion.x;
-						mwy = event.xmotion.y;
+						mx += ((int)event.xmotion.x - mwx);
+						my += ((int)event.xmotion.y - mwy);
 
 						if (mx || my)
 							dowarp = qtrue;
