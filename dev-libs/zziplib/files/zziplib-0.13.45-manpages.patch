--- zziplib-0.13.45/docs/Makefile.in
+++ zziplib-0.13.45/docs/Makefile.in
@@ -502,7 +502,7 @@
 	; echo '(rm man/*.3 ; rmdir man)' \
 	;       (rm man/*.3 ; rmdir man)  \
 	; fi ; test -s $@ || mv $@.old $@ \
-        || mv $(srcdir)/$@.old $@ || test -s $@.old >$@
+        || cp $(srcdir)/$@ $@ || test -s $@.old >$@
 
 htmpages.ar : zziplib.xml
 	@ if test "$(XMLTO)" = ":" \
