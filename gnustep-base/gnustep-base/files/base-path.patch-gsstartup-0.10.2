diff -u -r1.332 NSString.m
--- Source/NSString.m	22 Feb 2005 11:22:44 -0000	1.332
+++ Source/NSString.m	6 Apr 2005 17:41:03 -0000
@@ -3409,10 +3409,12 @@
 	{
 	  if (pathSepMember(buf[aLength]) == YES)
 	    {
+              buf[aLength] = '/';       // Standardise
 	      if (pathSepMember(buf[aLength-1]) == YES)
 		{
 		  unsigned	pos;
 
+                  buf[aLength-1] = '/'; // Standardise
 		  for (pos = aLength+1; pos < length; pos++)
 		    {
 		      buf[pos-1] = buf[pos];
