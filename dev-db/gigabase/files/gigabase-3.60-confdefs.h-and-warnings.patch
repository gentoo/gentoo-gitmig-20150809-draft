--- ./config.h.orig	2007-11-01 15:18:53.000000000 +0300
+++ ./config.h	2007-11-01 15:19:32.000000000 +0300
@@ -15,9 +15,6 @@
 #define _CRT_SECURE_NO_DEPRECATE true
 #endif
 
-// Use configuration file generated by configure script
-#include "confdefs.h"
-
 #ifndef AUTOCONFIG
 
 // USE_LOCALE_SETTINGS - use C locale for string comparison operations
@@ -136,12 +133,12 @@
 
 
 // Use reentrant version of localtime
-#if !defined(__APPLE__) && !defined(_WIN32)
+#if !defined(__APPLE__) && !defined(_WIN32) && !defined(HAVE_LOCALTIME_R)
 #define HAVE_LOCALTIME_R
 #endif
 
 // Use reentrant version of localtime
-#if !defined(__APPLE__) && !defined(_WIN32) && !defined(_AIX)
+#if !defined(__APPLE__) && !defined(_WIN32) && !defined(_AIX) && !defined(HAVE_GETHOSTBYNAME_R)
 #define HAVE_GETHOSTBYNAME_R
 #endif
 
