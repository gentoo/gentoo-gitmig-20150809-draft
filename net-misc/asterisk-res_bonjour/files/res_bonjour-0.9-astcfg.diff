--- res_bonjour/Makefile.orig	2005-08-27 23:29:38.000000000 +0000
+++ res_bonjour/Makefile	2005-08-27 23:30:34.000000000 +0000
@@ -9,15 +9,13 @@
 NAME=res_bonjour
 CONF=bonjour.conf
 
-CFLAGS=-g -Wall -D_REENTRANT -D_GNU_SOURCE -fPIC
+DESTDIR=
+
+CFLAGS+=-g -Wall -D_REENTRANT -D_GNU_SOURCE -fPIC
 LIBS=-ldns_sd
 RES=$(shell if [ -f /usr/include/asterisk/channel.h ]; then echo "$(NAME).so"; fi)
 
-ifeq (${OSARCH},Darwin)
-SOLINK=-dynamic -bundle -undefined suppress -force_flat_namespace
-else
-SOLINK=-shared -Xlinker -x
-endif
+SOLINK=$(shell /usr/bin/asterisk-config --solink)
 
 MODULES_DIR=/usr/lib/asterisk/modules
 
@@ -29,10 +27,6 @@
 clean:
 	rm -f $(NAME).o $(NAME).so
 
-install: all
-	if ! [ -f /etc/asterisk/$(CONF) ]; then \
-		install -m 644 $(CONF).sample /etc/asterisk/$(CONF) ; \
-	fi
-	if [ -f $(NAME).so ]; then \
-		install -m 755 $(NAME).so $(MODULES_DIR) ; \
-	fi
+install:
+	install -D -m 644 $(CONF).sample $(DESTDIR)/etc/asterisk/$(CONF)
+	install -D -m 755 $(NAME).so $(DESTDIR)$(MODULES_DIR)
