# Note that this requires portage-2.1.1+ so if you need this functionality,
# make sure your package forces a new-enough portage.

sys-devel/gcc hardened
sys-libs/glibc hardened

# Alexander "eroyf" Færøy <alexander.faeroey@gmail.com>
# Temporary masked due to QA issues with HAL (bug #166790).
# Removal of this entry is only allowed by the QA team. See GLEP 48.
sys-apps/pciutils zlib
