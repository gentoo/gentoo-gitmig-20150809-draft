diff -urdB dia-0.93-orig/configure dia-0.93/configure
--- dia-0.93-orig/configure	2004-06-05 02:22:52 -0500
+++ dia-0.93/configure	2004-06-05 02:58:20 -0500
@@ -23715,10 +23715,20 @@
 
 
 
-  PYTHON_PREFIX=${prefix}
+  if test "x$prefix" != "xNONE"; then
+    PYTHON_PREFIX=${prefix}
 
-  PYTHON_EXEC_PREFIX=${exec_prefix}
+  else
+    PYTHON_PREFIX=/usr
+
+  fi
+  if test "x$exec_prefix" != "xNONE"; then
+    PYTHON_EXEC_PREFIX=${exec_prefix}
 
+  else
+    PYTHON_EXEC_PREFIX=/usr
+
+  fi
 
 
 
