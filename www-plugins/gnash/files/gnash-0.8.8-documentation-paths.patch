diff -ur a/doc/C/Makefile.am b/doc/C/Makefile.am
--- a/doc/C/Makefile.am	2010-08-07 17:50:00.000000000 +0200
+++ b/doc/C/Makefile.am	2010-09-28 18:33:03.000000000 +0200
@@ -18,14 +18,12 @@
 
 # Building a PDF using Apache FOP only works with fop-0.20.5 for now.
 
-htmldir = ${prefix}/share/doc/gnash
-infodir = ${prefix}/share/info
-man1dir = ${prefix}/share/man/man1
+man1dir = ${mandir}/man1
 docname = gnash
 lang = C
 omffile = gnash-C.omf
 
-docdir = $(datadir)/$(PACKAGE)/doc/$(docname)/$(lang)
+# docdir = $(datadir)/$(PACKAGE)/doc/$(docname)/$(lang)
 omf_dest_dir=$(datadir)/omf/$(PACKAGE)
 scrollkeeper_localstate_dir = $(localstatedir)/scrollkeeper
 
