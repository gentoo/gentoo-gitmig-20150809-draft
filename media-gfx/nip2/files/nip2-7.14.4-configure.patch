diff -u nip2-7.14.4.orig/configure.in nip2-7.14.4/configure.in
--- nip2-7.14.4.orig/configure.in	2008-10-25 12:02:23.000000000 +0200
+++ nip2-7.14.4/configure.in	2008-10-25 12:06:25.000000000 +0200
@@ -222,13 +222,17 @@
 fi
 
 # optional ... use these to update desktop after install
+AC_ARG_ENABLE([mime-update], AS_HELP_STRING([--disable-mime-update], [ disable mime update (default: test)]))
+
 AC_PATH_PROG(UPDATE_MIME_DATABASE, update-mime-database, no)
 AC_PATH_PROG(UPDATE_DESKTOP_DATABASE, update-desktop-database, no)
 
 update=no
-if test "x$UPDATE_MIME_DATABASE" != "xno"; then
-  if test "x$UPDATE_DESKTOP_DATABASE" != "xno"; then
-    update=yes
+if test "x$enable_mime_update" != "xno"; then
+  if test "x$UPDATE_MIME_DATABASE" != "xno"; then
+    if test "x$UPDATE_DESKTOP_DATABASE" != "xno"; then
+      update=yes
+    fi
   fi
 fi
 
