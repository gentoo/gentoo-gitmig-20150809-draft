Index: gjl
===================================================================
--- src/gjl	(revision 2713)
+++ src/gjl	(revision 2714)
@@ -89,6 +89,11 @@
 
     classpath, library = get_pkg_args(pkg)
     if classpath:
+        envcp = os.environ['CLASSPATH']
+
+        if envcp:
+            classpath = ':'.join((envcp, classpath))
+
         args += ' -classpath %s' % (classpath)
 
     if library:
