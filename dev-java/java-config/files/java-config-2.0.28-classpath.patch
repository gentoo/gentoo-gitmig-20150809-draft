Index: src/gjl
===================================================================
--- src/gjl	(revision 2712)
+++ src/gjl	(working copy)
@@ -89,6 +89,11 @@
 
     classpath, library = get_pkg_args(pkg)
     if classpath:
+        envcp = os.getenv('CLASSPATH')
+
+        if envcp:
+            classpath = ':'.join((envcp, classpath))
+
         args += ' -classpath %s' % (classpath)
 
     if library:
