#!/bin/sh

if ! [ -d ~/.mupen64/save ]
then
	mkdir -p ~/.mupen64/{save,plugins}
	cd ~/.mupen64
	ln -s /usr/lib/mupen64/plugins/* plugins/
	ln -s /opt/mupen64/mupen64 mupen64
fi

cd $HOME/.mupen64
./mupen64	

