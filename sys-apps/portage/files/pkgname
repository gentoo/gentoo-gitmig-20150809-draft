#!/usr/bin/env spython

import portage
import sys

a=portage.pkgsplit(sys.argv[1])
if a:
	print a[0],a[1],a[2][1:]
	sys.exit(0)
else:
	sys.stderr.write(portage.ERRVER+"\n")
	sys.stderr.write(portage.ERRPKG+"\n")
	sys.exit(1)
