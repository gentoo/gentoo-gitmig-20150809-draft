#!/usr/bin/env python

from sys import *
import portage

mycommand=argv[1]+"("
x=2
while (x<len(argv)):
	if x==(len(argv)-1):
		mycommand=mycommand+'"'+argv[x]+'"'
	else:
		mycommand=mycommand+'"'+argv[x]+'",'
	x=x+1
mycommand=mycommand+")"
exec(mycommand)
