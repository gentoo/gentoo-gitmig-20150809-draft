for x in "$@"
do
	echo ">>> Unpacking ${x}"
	case "${x##*.}" in
	gz|tgz|Z|z) 
		tar xzvf ${x}
		;;
	bz2|tbz2)
		cat ${x} | bzip2 -d | tar xvf -
		;;
	ZIP|zip)
		unzip ${x}
		;;
	*)
		echo '!!!'" Error: couldn't unpack ${x}: file format not recognized"
		exit 1
		;;
	esac
done
