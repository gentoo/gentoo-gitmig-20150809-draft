--- busybox-1.4.2/Makefile.flags
+++ busybox-1.4.2/Makefile.flags
@@ -28,4 +28,8 @@
 ifeq ($(CONFIG_STATIC),y)
 LDFLAGS += -static
 endif
+
+ifeq ($(CONFIG_SELINUX),y)
+LDFLAGS += -lselinux -lsepol
+endif
 #LDFLAGS += -nostdlib
