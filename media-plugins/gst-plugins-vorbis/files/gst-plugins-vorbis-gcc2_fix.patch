diff -NurdB gst-plugins-0.8.2-orig/ext/vorbis/vorbisparse.c gst-plugins-0.8.2/ext/vorbis/vorbisparse.c
--- gst-plugins-0.8.2-orig/ext/vorbis/vorbisparse.c	2004-07-18 22:24:27 -0500
+++ gst-plugins-0.8.2/ext/vorbis/vorbisparse.c	2004-07-18 22:28:31 -0500
@@ -103,6 +103,9 @@
 vorbis_parse_set_header_on_caps (GstVorbisParse * parse, GstCaps * caps)
 {
   GstBuffer *buf1, *buf2, *buf3;
+  GstStructure *structure;
+  GValue list = { 0 };
+  GValue value = { 0 };
 
   g_assert (parse);
   g_assert (parse->streamheader);
@@ -115,9 +118,7 @@
   buf3 = parse->streamheader->next->next->data;
   g_assert (buf3);
 
-  GstStructure *structure = gst_caps_get_structure (caps, 0);
-  GValue list = { 0 };
-  GValue value = { 0 };
+  structure = gst_caps_get_structure (caps, 0);
 
   /* mark buffers */
   GST_BUFFER_FLAG_SET (buf1, GST_BUFFER_IN_CAPS);
