--- Makefile.in.orig	2003-08-17 17:32:15.000000000 +0900
+++ Makefile.in	2005-04-23 21:20:37.000000000 +0900
@@ -97,11 +97,11 @@
 	./config.status --recheck
 
 install: $(TARGETS)
-	$(srcdir)/mkinstalldirs $(bindir)
+	$(srcdir)/mkinstalldirs $(DESTDIR)$(bindir)
 	@list="$(TARGETS)"; for p in $$list; do \
 	  if test -f $$p; then \
-	    echo " $(INSTALL_PROGRAM) $$p $(bindir)/"; \
-             $(INSTALL_PROGRAM) $$p $(bindir)/; \
+	    echo " $(INSTALL_PROGRAM) $$p $(DESTDIR)$(bindir)/"; \
+             $(INSTALL_PROGRAM) $$p $(DESTDIR)$(bindir)/; \
           else :; fi; \
         done
 
@@ -110,8 +110,8 @@
 
 uninstall:
 	@list="$(TARGETS)"; for p in $$list; do \
-	echo " rm -f $(bindir)/$$p"; \
-	rm -f $(bindir)/$$p; \
+	echo " rm -f $(DESTDIR)$(bindir)/$$p"; \
+	rm -f $(DESTDIR)$(bindir)/$$p; \
         done
 
 clean:
