This was accepted and committed to ~mbiebl/ConsoleKit/ repository.

--- configure.ac
+++ configure.ac
@@ -249,6 +249,7 @@
 	if test -z "$UDEVDIR" ; then
 		UDEVDIR="/lib/udev"
 	fi
+	AC_SUBST(UDEVDIR)
 ])
 AM_CONDITIONAL([ENABLE_UDEV_ACL], [test "x$enable_udev_acl" = "xyes"])
 
