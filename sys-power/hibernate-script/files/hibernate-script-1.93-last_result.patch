Index: hibernate-script-1.93/scriptlets.d/suspend2
===================================================================
--- hibernate-script-1.93.orig/scriptlets.d/suspend2
+++ hibernate-script-1.93/scriptlets.d/suspend2
@@ -525,7 +525,7 @@ CleanupDmesg() {
 # PrintErrors. We can't just return it, because error codes may be > 255
 DoSuspend() {
     # Some versions of Software Suspend didn't clear this in certain cases:
-    echo 0 > $SWSUSP_ROOT/last_result
+    [ -w $SWSUSP_ROOT/last_result ] && echo 0 > $SWSUSP_ROOT/last_result
 
     if [ -f $SWSUSP_ROOT/do_suspend ] ; then
 	echo > $SWSUSP_ROOT/do_suspend
