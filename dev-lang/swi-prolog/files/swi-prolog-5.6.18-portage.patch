--- pl-5.6.18.orig/packages/Makefile.in	2006-08-30 20:35:12.000000000 +1200
+++ pl-5.6.18/packages/Makefile.in	2006-09-01 20:07:28.000000000 +1200
@@ -81,7 +81,7 @@
 
 install::
 	for p in $(PKG); do \
-	    if [ -r $$p/Makefile ]; then ( cd $$p && $(MAKE) $@ ); fi; \
+	    if [ -r $$p/Makefile ]; then $(MAKE) -C $$p $@ || exit 1; fi; \
         done
 	@echo "*** Forcing update of library index ***"
 	rm -f $(DESTDIR)$(PLLIB)/INDEX.pl
@@ -120,7 +120,7 @@
 	        echo "**********************"; \
 		echo "* Package $$p"; \
 		echo "**********************"; \
-	        ( cd $$p && $(CONFIG) ); \
+	        ( cd $$p && $(CONFIG) ) || exit 1; \
 	    fi; \
         done
 
@@ -137,7 +137,7 @@
 
 check::
 	for p in $(PKG); do \
-	    if [ -r $$p/Makefile ]; then ( cd $$p && $(MAKE) $@ ); fi; \
+	    if [ -r $$p/Makefile ]; then $(MAKE) -C $$p $@ || exit 1; fi; \
         done
 
 # test environment
