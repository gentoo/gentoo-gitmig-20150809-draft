#!/bin/sh
cd GENTOO_DIR
javabin="`which java 2>/dev/null`"
if [ -z "${javabin}" ] ; then
	echo "No 'java' binary found."
	echo "You should run 'java-config' to choose your default VM."
	exit 1
else
	exec java -jar cgoban.jar "$@"
fi
