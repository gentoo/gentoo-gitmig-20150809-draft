--- a/etc/init.d/vdr	(Revision 423)
+++ b/etc/init.d/vdr	(Arbeitskopie)
@@ -8,7 +8,7 @@
 include rc-functions
 
 depend() {
-	use lircd dvbsplash net coldplug
+	use lircd net coldplug
 	after checkroot
 }
 
@@ -35,7 +35,6 @@
 	#
 	# finally start vdr
 	# 
-	/usr/bin/dvbsplash stop 2>/dev/null >/dev/null
 	unset MAIL
 
 	vdr_home=/var/vdr
