diff -Naur klamav-0.07.orig/src/freshklam.cpp klamav-0.07/src/freshklam.cpp
--- klamav-0.07.orig/src/freshklam.cpp	2004-10-30 18:21:38.000000000 +0000
+++ klamav-0.07/src/freshklam.cpp	2004-12-28 22:09:21.759552800 +0000
@@ -70,7 +70,7 @@
 	lastDownloadPaths = config->readListEntry("lastDownloadPaths");
 
 	if (lastDownloadPaths.isEmpty()){
-		lastDownloadPaths.append( "/usr/local/share/clamav");
+		lastDownloadPaths.append( "/var/lib/clamav");
 		config->writeEntry("lastDownloadPaths", lastDownloadPaths);
 		lastDownloadPaths = config->readListEntry("lastDownloadPaths");
 	}
