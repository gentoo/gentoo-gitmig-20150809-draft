#!/sbin/runscript
depend() {
	before cupsd
	after hotplug
}

start() {
	ebegin "Starting hpoj"
	/usr/sbin/ptal-init start -q
	eend $?
}

stop() {
	ebegin "Stopping hpoj"
	/usr/sbin/ptal-init stop -q
	eend $?
}
