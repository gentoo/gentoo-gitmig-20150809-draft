#!/bin/bash

die() {
	echo "#####"
	echo $1
	echo "#####"
	exit 1
}

if [ $1 = "install" ]; then
	sed -e "s|/path/to/wikiconfig|${MY_INSTALLDIR}/wikiconfig.py|g" -i ${MY_INSTALLDIR}/moin.cgi || die "sed failed"
elif [ $1 = "clean" ]; then
	echo $1
fi
