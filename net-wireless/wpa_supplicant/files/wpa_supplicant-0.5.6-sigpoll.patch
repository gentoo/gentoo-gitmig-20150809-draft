--- ctrl_iface_dbus.h.orig	2006-11-25 09:01:16 +0000
+++ ctrl_iface_dbus.h	2006-11-25 09:00:53 +0000
@@ -15,6 +15,13 @@
 #ifndef CTRL_IFACE_DBUS_H
 #define CTRL_IFACE_DBUS_H
 
+/* If we don't have SIGPOLL, so use SIGIO instead */
+#ifndef SIGPOLL
+#ifdef SIGIO
+#define SIGPOLL SIGIO
+#endif
+#endif
+
 #ifdef CONFIG_CTRL_IFACE_DBUS
 
 #include <dbus/dbus.h>
