--- src/server/pbsd_init.c      (revision 1016)
+++ src/server/pbsd_init.c      (working copy)
@@ -1415,6 +1415,11 @@
   {
   struct stat sb;

+  if (*filename == '.')
+    {
+    return(-1);
+    }
+
   if (stat(filename,&sb) == -1)
     {
     return(errno);

