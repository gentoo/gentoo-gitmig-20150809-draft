diff -urN alsa-lib-0.9.0rc3/configure alsa-lib-0.9.0rc3-modified/configure
--- alsa-lib-0.9.0rc3/configure	Thu Aug 15 10:19:13 2002
+++ alsa-lib-0.9.0rc3-modified/configure	Sat Sep 14 10:10:38 2002
@@ -1647,8 +1647,8 @@
     aprefix=$ac_default_prefix
   fi
   if test -z "$soundbasedir" -a -r "$aprefix/include/sound"; then
-    ALSA_CFLAGS="$ALSA_CFLAGS -I$aprefix/include"
-    CFLAGS="$CFLAGS -I$aprefix/include"
+    ALSA_CFLAGS="$ALSA_CFLAGS"
+    CFLAGS="$CFLAGS"
     echo "$ac_t""$ALSA_CFLAGS" 1>&6
   else
     echo "$ac_t"""not specified - using C compilator defaults"" 1>&6
