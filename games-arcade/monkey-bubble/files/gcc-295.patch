--- monkey-bubble-0.1.5/src/view/gdk-view.c.orig	2003-10-03 04:40:26.000000000 +0200
+++ monkey-bubble-0.1.5/src/view/gdk-view.c	2003-10-03 04:42:27.000000000 +0200
@@ -934,14 +934,12 @@
 
 
 void gdk_view_set_score(GdkView * d,int score) {
-	 Block * block;
-  g_assert(IS_GDK_VIEW(d));
-  gdk_view_clear_score(d);
-
-
+  Block * block;
   int pre_score;
   int i = 0;
-  
+
+  g_assert(IS_GDK_VIEW(d));
+  gdk_view_clear_score(d);
 
   if( score != 0 ) {
 
