--- src/pdbparse.c.old	2005-03-25 12:12:50.000000000 -0500
+++ src/pdbparse.c	2005-03-25 10:55:57.000000000 -0500
@@ -240,7 +240,7 @@
 	
 	/* Parse pdb file and write pdb structure */
 	if(!(pdb=ajPdbReadRawNew(pdb_inf, pdbid, min_chain_size, max_mismatch, 
-				 camask, camask1, atommask, logf)))
+				 0, camask, camask1, atommask, logf)))
 	{	
 	    ajFmtPrintS(&msg, "Clean coordinate file not generated for %S", temp);
 	    ajWarn(ajStrStr(msg));
--- src/scopnr.c.old	2005-03-25 12:17:40.000000000 -0500
+++ src/scopnr.c	2005-03-25 11:11:28.000000000 -0500
@@ -324,10 +324,10 @@
 		    /* Remove redundancy from list_seqs*/
 		    if(moden==1)
 			ret=embDmxSeqNR(list_seqs, &keep, &nsetnr, matrix, gapopen, 
-					   gapextend,thresh);
+					   gapextend,thresh, FALSE);
 		    else
 			ret=embDmxSeqNRRange(list_seqs, &keep, &nsetnr, matrix, gapopen, 
-					   gapextend,threshlow, threshup);		    
+					   gapextend,threshlow, threshup, FALSE);		    
 		    if(!ret)
 		    {
 			ajStrDel(&last_fam);
@@ -464,10 +464,10 @@
 	/* Remove redundancy from list_seqs*/
 	if(moden==1)
 	    embDmxSeqNR(list_seqs, &keep, &nsetnr, matrix, gapopen, 
-			   gapextend,thresh);		
+			   gapextend,thresh, FALSE);		
 	else
 	    embDmxSeqNRRange(list_seqs, &keep, &nsetnr, matrix, gapopen, 
-				gapextend,threshlow, threshup);		
+				gapextend,threshlow, threshup, FALSE);		
 
 	/* Write file with SCOP entries that are retained*/
 	for(iter=ajListIterRead(list_scop), x=0;
--- src/seqnr.c.old	2005-03-25 12:19:34.000000000 -0500
+++ src/seqnr.c	2005-03-25 11:13:42.000000000 -0500
@@ -829,7 +829,7 @@
 
         /* Remove the redundancy from the sequence set */
 /*JISON*/        keep        = ajIntNew();
-        embDmxSeqNR(famin_seqs, &keep, &nsetnr, matrix, gapopen, gapextend, thresh);
+        embDmxSeqNR(famin_seqs, &keep, &nsetnr, matrix, gapopen, gapextend, thresh, FALSE);
 
         /* clean up famin_seqs */
         iter=ajListIterRead(famin_seqs);
