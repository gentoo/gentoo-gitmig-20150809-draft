diff -NurpP eix-0.3.0/src/eix-sync.in eix-0.3.0-tmpfile/src/eix-sync.in
--- eix-0.3.0/src/eix-sync.in	2005-08-26 23:29:36.000000000 +0200
+++ eix-0.3.0-tmpfile/src/eix-sync.in	2005-11-18 00:44:16.427797250 +0100
@@ -42,8 +42,10 @@ function die() {
 
 [ "$UID" -eq 0 ] || die "You need to be root."
 
-emerge --sync                      || die "Error syncing."
-cp /var/cache/eix /tmp/eix.$$.sync || die "Error while copying cache-file to a save location."
-update-eix                         || die "Problems with update-eix."
-diff-eix /tmp/eix.$$.sync          || die "Error while diffing."
+tmpfile=$(mktemp)
+
+emerge --sync                || die "Error syncing."
+cp /var/cache/eix ${tmpfile} || die "Error while copying cache-file to a save location."
+update-eix                   || die "Problems with update-eix."
+diff-eix ${tmpfile}          || die "Error while diffing."
 exit 0
