# These lines below will start the TeaPOP - POP Server
service pop-3
{
        socket_type     = stream
        protocol        = tcp
        wait            = no
        user            = root
        server          = /usr/sbin/teapop
        server_args     = teapop
        disable         = no
}
